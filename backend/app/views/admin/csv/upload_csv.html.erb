<%= form_for :dump, :url => {:action => "import_csv"}, :html => {:multipart => true} do |f| %>
  <p>
    Select a CSV File : <%= f.file_field :file %>
  </p>
  <p>
    <%= submit_tag 'Submit' %>
  </p>
<% end %>